# M-Journey 백엔드 개발 작업 목록

상세 API 계약은 [M-Journey_백엔드_기능_명세서.md](./M-Journey_백엔드_기능_명세서.md)를 기준으로 한다.

## 1단계 — 기본 코어 (이번 구현)

- [x] FastAPI 프로젝트 및 환경 설정
- [x] SQLite/PostgreSQL 교체 가능한 SQLAlchemy 연결
- [x] 고객, 상품, 매장, 재고, QR/NFC 진입 태그 seed 데이터
- [x] 서버 생존/준비 상태 API
- [x] 고객 프로필 조회 API
- [x] 매장 및 상품/재고 조회 API
- [x] QR 기본 매장 체크인 생성·조회·취소 API
- [x] QR 진입 URL 검증과 프론트 체크인 화면 리다이렉트
- [x] 프라이빗/직원 응대 쇼핑 방식 선택 API
- [x] 개인정보 공유 동의와 방문 목적 저장 API
- [x] 공통 오류 형식과 요청 ID
- [x] 핵심 API 자동 테스트

## 2단계 — 인증과 직원 기능

- [x] 고객/직원 사용자 테이블과 비밀번호 해시
- [x] JWT Access/Refresh Token 로그인·갱신·로그아웃
- [x] 고객/직원 역할 분리와 고객 API 접근 제어
- [x] 직원 API의 매장별 접근 제어
- [x] 직원 방문 대기열 조회
- [x] 직원 요청 수락 및 중복 배정 방지 트랜잭션
- [x] 방문 진행·완료 처리
- [x] 직원용 고객 정보 마스킹

## 3단계 — AI 연동

- [x] 교체 가능한 AI Provider 인터페이스와 기본 Provider
- [ ] 실제 AI 저장소 함수 또는 HTTP API Provider 연결
- [x] 고객·방문 목적·재고 컨텍스트 구성
- [x] 고객용 룩북 생성 API
- [x] 직원용 응대 가이드 API
- [x] Pydantic 기반 AI 출력 검증
- [x] 실제 상품 ID·가격·매장 재고 재검증
- [x] AI 타임아웃, 재시도, 실패 fallback
- [x] 추천 결과 저장 및 캐시

## 4단계 — 실시간 연동

- [x] 직원 대시보드 WebSocket 인증
- [x] 방문 대기, AI 완료, 직원 배정, 취소·완료 이벤트
- [x] 고객 화면에 직원 배정 결과 실시간 전달
- [x] 연결 복구용 REST 전체 상태 동기화
- [ ] 다중 서버 배포 시 Redis Pub/Sub 적용

## 5단계 — 부가 기능과 운영 준비

- [x] 찜 목록, 추천 홈, 구매 이력 API
- [x] 일회용 토큰 기반 비밀번호 재설정과 기존 인증 토큰 폐기
- [x] 개인정보 동의 철회와 직원 접근 즉시 차단
- [ ] 개인정보 보존 기간 확정 및 만료 데이터 purge
- [x] 보안·권한 변경 감사 로그와 HTTP 구조화 로그
- [x] 로그인·비밀번호 재설정·AI 생성 rate limit
- [x] 운영 CORS HTTPS 화이트리스트 검증과 최소 메서드·헤더 설정
- [ ] 실제 프론트 배포 도메인 확정 및 운영 환경변수 반영
- [x] Alembic 초기 마이그레이션과 SQLite upgrade/downgrade/model diff 검증
- [x] 실제 PostgreSQL 인스턴스 마이그레이션 적용 검증
- [x] Docker API·PostgreSQL 로컬 실행 구성
- [x] 실제 Docker 기동과 PostgreSQL migration·seed 통합 검증
- [ ] 배포 주소 기반 실제 QR 생성·출력과 전체 시연 리허설

## 현재 개발용 제약

- 고객 식별은 JWT Access Token의 `sub`를 사용한다.
- QR이 기본 진입 방식이며 토큰은 `M_JOURNEY_DEMO_QR_TOKEN`으로 설정한다.
- NFC 호환 seed 토큰 `nfc-demo-seoul-001`도 유지한다.
- seed 데이터는 모두 가상 데이터다.
- 기본 DB는 `backend/mjourney.db` SQLite 파일이다.
- 실제 외부 AI Provider 연결과 다중 서버용 Redis 이벤트 전달은 아직 구현 범위가 아니다.

## 남은 작업 우선순위

### 백엔드에서 다음으로 진행

1. 실제 프론트 배포 도메인 확정 및 운영 CORS 환경변수 반영
2. 개인정보 보존 정책 확정 후 만료 데이터 purge

### 외부 산출물 준비 후 연결

1. 실제 AI 함수 또는 HTTP API를 `AIProvider` 구현으로 연결
2. 비밀번호 재설정 메일 Provider 연결
3. 프론트 REST·WebSocket 연결과 오류/재연결 UX 검증
4. 실제 QR URL과 배포 주소 확정 후 QR 출력·전체 시연 리허설

### 확장 시 적용

- 다중 백엔드 인스턴스가 필요할 때 Redis Pub/Sub 적용
- 실제 CRM·재고 시스템과 구매 이력 연동
